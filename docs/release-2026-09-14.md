# 2026-09-14 发布前核对

本次按用户授权，在同步云端文章、检查部署兼容性后发布结构与加载优化。

- GitHub `source` 已快进至 `ab62f6f0a9148e1c5345a2ae37006c7f47b24848`，新增 9 月 9–14 日六篇简报；本地共 40 篇文章，文章文件与云端一致，未发生合并冲突。
- 同步 `translations` 分支缓存后，构建生成 101 个中文页面和 101 个英文页面，未发现缺少翻译的片段，本地没有调用付费翻译服务。
- GitHub Actions 负责从 `source` 构建并更新 `main`；Vercel 生产部署取自 `main`，使用 Node 22、香港区域。源码和翻译分支的其他部署是预览，不承担博客域名流量。
- 域名 `blog.adenxie.com.cn` 的 CNAME 指向 Vercel。GitHub Pages 项目入口 `https://adenxie.github.io/Aden.github.io/` 跳转到该博客域名。没有修改 DNS 或生产分支设置。
- 两个 API 文件与发布前 `main` 分支逐字节一致；天气接口实测返回 200。汇率接口在发布前重复返回 `502 / source_timeout`，属于既有中国银行上游请求超时，不是此次静态资源或构建改动造成；客户端保留明确错误状态和缓存处理。
- 58 项测试通过，202 个页面的资源引用检查通过；手机及桌面功能检查通过，包含搜索失败重试、双语搜索、评论按需初始化、音乐请求时机、主题切换和页面往返。
- 本机 Git HTTPS 凭据已失效，发布使用已授权的 GitHub 连接；不改写远程 `source` 历史，不覆盖后来新增的文章。

发布前生产成品提交：`256a554f7c38075a9ab584296b8dce49c79b8283`。

发布前 Vercel 生产部署：`dpl_EEpZpP4x4bV1t7T3tFX613ggrqP8`。该部署仍可用于回退；最终是否发布成功，以本次 GitHub Actions、GitHub Pages、Vercel 部署记录及线上资源核验结果为准。
