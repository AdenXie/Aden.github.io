<% if (theme.comment.system === 'twikoo' && theme.comment.config.twikoo.server_url) { %>
<div class="twikoo-container">
  <div id="twikoo-comment" style="min-height:220px" data-env="<%= theme.comment.config.twikoo.server_url %>" data-region="<%= theme.comment.config.twikoo.region || '' %>"></div>
</div>
<% } %>
